<?xml version="1.0"?>
<launch>
  <arg name="base" default="static"/>

  <!-- By default we do not overwrite the URDF. Change the following to true to change the default behavior -->
  <arg name="load_robot_description" default="false"/>

  <!-- The name of the parameter under which the URDF is loaded -->
  <arg name="robot_description_param_name" default="robot_description"/>

  <!-- Load universal robot description format (URDF) -->
  <group if="$(arg load_robot_description)">
    <include file="$(find ur_with_cam_gazebo)/launch/inc/load_ur_with_cam.launch.xml">
      <arg name="robot_description_param_name" value="$(arg robot_description_param_name)" />
      <arg name="base" value="$(arg base)" />
    </include>
  </group>

  <!-- The semantic description that corresponds to the URDF -->
  <param name="$(arg robot_description_param_name)_semantic" textfile="$(find ur_with_cam_gazebo)/config/$(arg base)/ur_with_cam.srdf" />

  <!-- Load updated joint limits (override information from URDF) -->
  <group ns="$(arg robot_description_param_name)_planning">
    <rosparam command="load" file="$(find ur_with_cam_gazebo)/config/$(arg base)/joint_limits.yaml"/>
  </group>

  <!-- Load default settings for kinematics; these settings are overridden by settings in a node's namespace -->
  <group ns="$(arg robot_description_param_name)_kinematics">
    <rosparam command="load" file="$(find ur_with_cam_gazebo)/config/$(arg base)/kinematics.yaml"/>

  </group>

</launch>
